class Solution {
public:
    void reverseString(vector<char>& s) {
        int n=s.size();
        int l=0;
        while(l<n){
            swap(s[l],s[n-1]);
            l++;
            n--;
        }
    }
};
